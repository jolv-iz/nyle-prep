# Copy this file to .env (already gitignored) and fill in your real
# Supabase Auth credentials — the same email/password you use to sign into
# the live site. Used only by scripts/next-day.js to read your real drill
# history from Supabase, locally, on this machine. Never committed, never
# sent anywhere but Supabase's own auth endpoint.
SUPABASE_EMAIL=
SUPABASE_PASSWORD=
